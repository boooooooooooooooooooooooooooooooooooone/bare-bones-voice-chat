pluginManagement {
    repositories {
        gradlePluginPortal()
        maven { url = 'https://maven.fabricmc.net/' }
    }
}

rootProject.name = 'BareBonesVoiceChat'

include(
        "common",
        "voice-client",
        "voice-server",
        "fabric-1.21.11",
        "fabric-26.1.2"
)
