pluginManagement {
    repositories {
        gradlePluginPortal()
        maven { url = 'https://maven.fabricmc.net/' }
    }
}

rootProject.name = 'BareBonesVoiceChat'

include("common", "voice-client", "voice-server")