plugins {
    id 'java'
}

version = project.version
group = project.maven_group

dependencies {
    implementation project(':common')
}

jar {
    archiveBaseName.set('barebonesvc-client')
    from(project(":common").sourceSets.main.output)
}