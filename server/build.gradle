plugins {
    id 'java'
}

version = '1.0.0'
group = 'xyz.pobob.barebonesvc'

repositories {
    mavenCentral()
}

dependencies {
    implementation project(':common')
}

jar {
    manifest {
        attributes(
                'Main-Class': 'xyz.pobob.barebonesvc.BareBonesVCServer'
        )
    }
    archiveBaseName.set('barebonesvcserver')
    from(project(":common").sourceSets.main.output)
}